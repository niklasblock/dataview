[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dataview"
version = "0.1.0"
description = "A Python-based storage analysis and file intelligence platform."
requires-python = ">=3.10"

[project.scripts]
dataview = "dataview.cli:main"